# Telegram bot — from @BotFather
BOT_TOKEN=

# Personal Telegram chat_id of the bot owner — used as the destination for
# private status replies (response_poller in recommendations_bot.py).
USER_CHAT_ID=

# Anthropic API key — used by scanner.py for thread → recommendations
# extraction. Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
